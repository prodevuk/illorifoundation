---
import { Image } from 'astro:assets';
import headerImage from '../assets/images/header_image.png';
---

<section class="relative bg-primary text-bone min-h-[80vh] flex items-center overflow-hidden">
  
  <!-- Background Image covering the entire hero section -->
  <div class="absolute inset-0 w-full h-full z-0">
    <Image 
      src={headerImage} 
      alt="Mother holding child lovingly" 
      class="object-cover w-full h-full mix-blend-overlay opacity-60"
    />
    <!-- Gradient Overlay for readability and brand tint -->
    <div class="absolute inset-0 bg-gradient-to-r from-primary via-primary/90 to-primary/60 mix-blend-multiply"></div>
  </div>

  <div class="container mx-auto px-6 md:px-12 relative z-10">
    
    <!-- Content -->
    <div class="space-y-8 max-w-lg">
      <div class="border border-accent p-8 md:p-10 relative bg-primary/20 backdrop-blur-sm">
        <!-- Decorative Gold Corner? Optional but fits the style -->
        <h1 class="font-serif text-4xl md:text-5xl leading-tight mb-4">
          Begin with <span class="text-accent italic">Care.</span>
        </h1>
        <p class="font-sans text-lg text-bone/90 leading-relaxed">
          The first 1,000 days define a lifetime. We provide essential nutrition and healthcare to mothers and infants in the regions that need it most.
        </p>
      </div>
      
      <div class="flex gap-4">
        <a href="#" class="bg-accent text-primary font-bold py-3 px-8 rounded-sm hover:bg-white transition-colors">
          Start Giving
        </a>
        <a href="#" class="border border-bone text-bone py-3 px-8 rounded-sm hover:bg-bone hover:text-primary transition-colors">
          Learn More
        </a>
      </div>
    </div>

  </div>
</section>
